# discord builds tracker

This repo tracks builds that were missed by datamining repos, this could also miss builds! (it works every 1min)

This repo only posts build numbers and version hashes, no html, no js files are published, as they can be retrieved using the overlay method
